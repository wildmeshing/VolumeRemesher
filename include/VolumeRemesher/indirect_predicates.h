#pragma once

// ---------------------------------------------------------------------------
// Indirect_Predicates' exact/indirect predicates, defined INSIDE namespace vol_rem.
// See VolumeRemesher/numerics.h for why. implicit_point.h already includes
// indirect_predicates.h inside the namespace, so this is just the historical path.
// ---------------------------------------------------------------------------

#include <VolumeRemesher/implicit_point.h>
